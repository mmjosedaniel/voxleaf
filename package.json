{
  "name": "voxleaf",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": "^22.12.0 || ^24.0.0",
    "pnpm": "11.15.1"
  },
  "scripts": {
    "benchmark:tts:measure": "uv run --directory services/tts --locked python -m benchmarks.cli measure",
    "benchmark:tts:preflight": "uv run --directory services/tts --locked python -m benchmarks.cli preflight",
    "benchmark:tts:cpu-fallback:assess": "uv run --directory services/tts --locked python -m benchmarks.cpu_fallback_result_cli assess",
    "benchmark:tts:cpu-fallback:derive": "uv run --directory services/tts --locked python -m benchmarks.cpu_fallback_result_cli derive",
    "benchmark:tts:prototype": "uv run --directory services/tts --project benchmarks/candidates/qwen3_1_7b_customvoice_cuda --locked python -m benchmarks.incremental_prototype_cli run",
    "benchmark:tts:batch": "uv run --directory services/tts --project benchmarks/candidates/qwen3_1_7b_customvoice_cuda --locked python -m benchmarks.batch_cli run",
    "benchmark:tts:batch:derive": "uv run --directory services/tts --locked python -m benchmarks.batch_result_cli derive",
    "benchmark:tts:dual-worker": "uv run --directory services/tts --project benchmarks/candidates/qwen3_1_7b_customvoice_cuda --locked python -m benchmarks.dual_worker_cli run",
    "benchmark:tts:dual-worker:derive": "uv run --directory services/tts --locked python -m benchmarks.dual_worker_result_cli derive",
    "benchmark:tts:quality:aggregate": "uv run --directory services/tts --locked python -m benchmarks.quality_cli aggregate",
    "benchmark:tts:quality:cleanup": "uv run --directory services/tts --locked python -m benchmarks.quality_cli cleanup",
    "benchmark:tts:quality:correct": "uv run --directory services/tts --locked python -m benchmarks.quality_cli correct",
    "benchmark:tts:quality:finalize": "uv run --directory services/tts --locked python -m benchmarks.quality_cli finalize",
    "benchmark:tts:quality:generate": "uv run --directory services/tts --locked python -m benchmarks.quality_cli generate",
    "benchmark:tts:quality:submit": "uv run --directory services/tts --locked python -m benchmarks.quality_cli submit",
    "benchmark:tts:screen:cleanup": "uv run --directory services/tts --locked python -m benchmarks.customvoice_screen_cli cleanup",
    "benchmark:tts:screen:generate": "uv run --directory services/tts --project benchmarks/candidates/qwen3_1_7b_customvoice_cuda --locked python -m benchmarks.customvoice_screen_cli generate",
    "benchmark:tts:screen:submit": "uv run --directory services/tts --locked python -m benchmarks.customvoice_screen_cli submit",
    "benchmark:reader": "pnpm run build:packages && pnpm --filter @voxleaf/desktop benchmark:reader",
    "benchmark:reader:native": "pnpm run build:packages && pnpm --filter @voxleaf/desktop benchmark:reader:native",
    "audit:release": "node scripts/release-audit.mjs",
    "build": "pnpm run build:packages && pnpm run build:desktop && pnpm run build:python",
    "build:desktop": "pnpm --filter @voxleaf/desktop tauri build",
    "build:packages": "pnpm --filter @voxleaf/epub --filter @voxleaf/shared build",
    "build:portable": "pnpm run build:packages && pnpm --filter @voxleaf/desktop build && pnpm run build:python",
    "build:python": "uv build services/tts",
    "check": "pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build",
    "check:portable": "pnpm run format:check:typescript && pnpm run format:check:python && pnpm run lint:typescript && pnpm run lint:python && pnpm run typecheck && pnpm run test:typescript && pnpm run test:python && pnpm run build:portable",
    "format": "pnpm run format:typescript && pnpm run format:rust && pnpm run format:python",
    "format:check": "pnpm run format:check:typescript && pnpm run format:check:rust && pnpm run format:check:python",
    "format:check:python": "uv run --project services/tts --locked ruff format --check services/tts",
    "format:check:rust": "cargo fmt --check --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "format:check:typescript": "prettier --check \"**/*.{css,js,json,ts,tsx,yaml,yml}\" \"!tmp/**\" \"!temp/**\" \"!**/.pytest_cache/**\" \"!**/.mypy_cache/**\" \"!**/.ruff_cache/**\" \"!**/__pycache__/**\"",
    "format:python": "uv run --project services/tts --locked ruff format services/tts",
    "format:rust": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "format:typescript": "prettier --write \"**/*.{css,js,json,ts,tsx,yaml,yml}\"",
    "lint": "pnpm run lint:typescript && pnpm run lint:rust && pnpm run lint:python",
    "lint:python": "uv run --project services/tts --locked ruff check services/tts",
    "lint:rust": "cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets -- -D warnings",
    "lint:typescript": "eslint .",
    "test": "pnpm run test:typescript && pnpm run test:rust && pnpm run test:python",
    "test:browser": "pnpm run build:packages && pnpm --filter @voxleaf/desktop test:browser",
    "test:browser:install": "pnpm --filter @voxleaf/desktop test:browser:install",
    "test:native-startup": "pnpm --filter @voxleaf/desktop test:native-startup",
    "test:tts:exact-host": "pnpm --filter @voxleaf/desktop test:tts:exact-host",
    "test:tts:adaptive-exact-host": "pnpm --filter @voxleaf/desktop test:tts:adaptive-exact-host",
    "test:tts:resilience-exact-host": "pnpm --filter @voxleaf/desktop test:tts:resilience-exact-host",
    "test:tts:bilingual-profiles-exact-host": "pnpm --filter @voxleaf/desktop test:tts:bilingual-profiles-exact-host",
    "test:tts:bilingual-portfolio-exact-host": "pnpm --filter @voxleaf/desktop test:tts:bilingual-portfolio-exact-host",
    "test:tts:handoff-host": "pnpm --filter @voxleaf/desktop tauri build && uv run --directory services/tts --locked python -m benchmarks.service_handoff_cli run",
    "test:python": "uv run --project services/tts --locked pytest services/tts",
    "test:rust": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "test:typescript": "pnpm --recursive --if-present test",
    "typecheck": "pnpm run typecheck:typescript && pnpm run typecheck:python",
    "typecheck:python": "uv run --directory services/tts --locked mypy .",
    "typecheck:typescript": "pnpm run build:packages && pnpm --recursive --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.1.1",
    "eslint": "10.7.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.3",
    "globals": "17.7.0",
    "json-schema-to-typescript": "15.0.4",
    "prettier": "3.9.5",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
