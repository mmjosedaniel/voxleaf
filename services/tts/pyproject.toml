[build-system]
requires = ["uv_build>=0.11.29,<0.13"]
build-backend = "uv_build"

[project]
name = "voxleaf-tts"
version = "0.0.0"
description = "Local TTS service foundation for VoxLeaf"
requires-python = ">=3.12,<3.13"
dependencies = [
    "jsonschema>=4.25,<5",
    "referencing>=0.37,<0.38",
]

[dependency-groups]
dev = [
    "mypy>=2.3.0,<3",
    "pytest>=9.1.1,<10",
    "ruff>=0.15.22,<0.16",
    "types-jsonschema>=4.26,<5",
]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "benchmarks", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]
